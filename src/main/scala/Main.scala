package runtime

object Main {

  def calfb (num: Int) = {
    
  }

  // should accept a flag which lets you skip the pre-processing steps
  def main(args: Array[String]) = {

    (1 to 100).foreach(calcfb)
  }
}


