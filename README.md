This is an exercise in information retrieval using the Scala programming language. To execute the task, simply run `sbt run`

Some thoughts
 - prioritize #s
 - flag for skipping the preprocessing step (or different run configs)

